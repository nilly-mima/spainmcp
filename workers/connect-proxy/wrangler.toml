name = "spainmcp-connect"
main = "src/index.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

[vars]
SUPABASE_URL = "https://xjumlmtepbzebuxbhksu.supabase.co"

# Secrets (set via `wrangler secret put`):
# SUPABASE_SERVICE_ROLE_KEY

# KV namespace for OAuth session state
# [[kv_namespaces]]
# binding = "OAUTH_KV"
# id = "<create with: wrangler kv namespace create OAUTH_KV>"

# Durable Objects for stateful MCP sessions (Phase 2+)
# [durable_objects]
# bindings = [
#   { name = "MCP_SESSION", class_name = "McpSession" }
# ]
# [[migrations]]
# tag = "v1"
# new_classes = ["McpSession"]
